name: start-sonarqube

on:
  schedule:
    - cron: '45 15 * * *'

jobs:
  start_sonarqube:
    runs-on: ubuntu-latest
    steps:
      - name: Discord
        uses: Ilshidur/action-discord@master
        with:
          args: 'The peer team sucks'